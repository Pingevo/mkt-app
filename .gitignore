# Secrets & environment
.env
.env.local
.env.*.local

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
.Python

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Logs & temp
*.log
/tmp/

# Output / generated deliverables
output/

# Customer data (real product data — keep examples only)
data/Lagenio*/
cache/Lagenio*/

# Test / mockup data (not for production)
data/Mockup*/
cache/Mockup*/
data/Coffee/
cache/Coffee/
data/ZMI*/
cache/ZMI*/
mockup_data/

# Brand profiles (real customer data — keep .example.md + .example.json only)
brand/brand_profile.md
brand/tone_of_voice.md
brand/target_audience.md
brand/visual_guidelines.md
brand/*.md.bak
brand/voice.json
brand/terms.json
brand/visual.json
brand/audience.json

# Asset Library — user uploaded files + catalog DB (system space)
brand/assets/
cache/assets/

# Scheduler — job store + run log (user data, not templates)
cache/scheduled_jobs.json
cache/scheduled_runs.json

# Content history — runtime state, เปลี่ยนทุกครั้งที่รัน
cache/content_history.json

# Runtime qualification / debug outputs
data/all_agents_beta_qualification/
cache/all_agents_beta_qualification/
data/campaign_*/
cache/campaign_*/
data/CACGO*/
cache/CACGO*/
data/.staging/

# Runtime evaluation / preflight / dryrun artifacts (not source of truth)
data/campaign_*_report.md
data/campaign_*_state.json
data/campaign_*_plan*.json
data/campaign_*_decision_record.md
data/campaign_*_scenarios.json
data/campaign_*_readiness.md
data/campaign_real_eval_plan.json
data/campaign_paid_acceptance_plan.md
data/agent3_offline_stabilization_report.md

# Consultation / audit scratch notes (not source of truth)
audit_*.md
consult_*.md

# IDE
.windsurf/

# Temporary mockups
staging_preview_mockup.html

# Diagnostic scripts (one-off experiments)
tests/diagnose_*.py

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~
logs/
